import React from 'react';
import './MyComponent.css'; // Import the CSS file

const MyComponent = () => {
    return (
        <div className="my-component"> {/* Apply the class here */}
            <h1>Tiburon Cloud Partners</h1>
        </div>
    );
};

export default MyComponent;
